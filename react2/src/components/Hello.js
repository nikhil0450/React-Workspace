import React from "react";

const Hello = () =>{

    //This is the example of JSX
    // return <h1>Hello Nikhil</h1>

    // This is the example without using JSX
    // return React.createElement('div', {id: "div-tag"}, React.createElement("h1", {className: "header"}, "Hello Nikhil"))
    // ( 'tag_name', 'attribute as a object', 'text we want to pass' )
}

export default Hello